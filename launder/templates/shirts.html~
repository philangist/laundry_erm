shirts
{% if orders %}
    <ul>
    {% for order in orders %}
        {{ order.first_name }}
        {{ order.last_name }}
        {{ order.phone_number }}
    {% endfor %}
    </ul>
{% else %}
    <p>No orders are available.</p>
{% endif %}